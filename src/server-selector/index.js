const { parseAllProviders, getProvidersForInstance } = require('./providers');
const { registerRoutes } = require('./routes');

module.exports = { parseAllProviders, getProvidersForInstance, registerRoutes };
